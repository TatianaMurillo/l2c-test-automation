<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Billing_Country_Required</fullName>
    <active>true</active>
    <description>Blocks saving an Account without a Billing Country. Uses BillingCountryCode (requires State and Country/Territory Picklists enabled). If your org uses free-text country instead, swap for ISBLANK(BillingCountry).</description>
    <errorConditionFormula>ISBLANK(BillingCountryCode)</errorConditionFormula>
    <errorMessage>Billing Country is required.</errorMessage>
</ValidationRule>
