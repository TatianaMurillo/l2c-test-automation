<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Billing_Postal_Code_Format</fullName>
    <active>true</active>
    <description>Validates US postal code format on Account's Billing Address - covers the "Address" module (no standalone Address object in Salesforce; addresses live as compound fields on Account/Contact). Replicate for Shipping/Contact if needed.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(BillingCountryCode, "US"),
    NOT(ISBLANK(BillingPostalCode)),
    NOT(REGEX(BillingPostalCode, "^\\d{5}(-\\d{4})?$"))
)</errorConditionFormula>
    <errorMessage>Invalid postal code format. US postal codes must match 12345 or 12345-6789.</errorMessage>
</ValidationRule>
