<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Order_Contract_Required_For_Activation</fullName>
    <active>true</active>
    <description>Prevents activating an Order with no related Contract. Assumes "Activated" is a value on Order.Status in this org - confirm the exact label in Setup -> Object Manager -> Order -> Fields -> Status before deploying.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status, "Activated"),
    ISBLANK(ContractId)
)</errorConditionFormula>
    <errorMessage>Contract is required to activate an order.</errorMessage>
</ValidationRule>
