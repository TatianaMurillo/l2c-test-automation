<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Quote_LineItem_Required_For_Approval</fullName>
    <active>true</active>
    <description>Prevents approving a Quote that has no line items, since an Order created from it later would be empty. Depends on the Total_Quote_Lines__c roll-up field.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status, "Approved"),
    Total_Quote_Lines__c = 0
)</errorConditionFormula>
    <errorMessage>At least one line item is required to approve a quote.</errorMessage>
</ValidationRule>
